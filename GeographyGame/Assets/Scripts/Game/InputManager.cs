using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.XR;

public class InputManager : MonoBehaviour
{
    /*
    public List<InputDevice> inputDevices = new List<UnityEngine.XR.InputDevice>();

    public bool triggerPressed;
    public Vector2 rotationThumbstick;

   

    // Start is called before the first frame update
    void Start()
    {
        InputDevices.GetDevices(inputDevices);
    }

    // Update is called once per frame
    void Update()
    {
        inputDevices[0].TryGetFeatureValue(CommonUsages.triggerButton, out triggerPressed);
        inputDevices[1].TryGetFeatureValue(CommonUsages.primary2DAxis,  out rotationThumbstick);
    }
    */
}
